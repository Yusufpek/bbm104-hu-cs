/**
 * A main class representing the Duck Hunt game.
 * Extends the DuckApplication class.
 */
public class DuckHunt extends DuckApplication {
    final static double SCALE = 3; // Default: 3
    final static double VOLUME = 0.025; // Default: 0.025

    /**
     * The main entry point for the Duck Hunt game.
     * Launches the application.
     *
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        launch(args);
    }
}